{
  "name": "matte",
  "version": "1.0.0",
  "private": true,
  "description": "WebGPU organic transition-matte and ambient-field generator (no build; static site)",
  "type": "module",
  "scripts": {
    "check": "node test/check-shaders.mjs",
    "smoke": "node test/smoke.mjs",
    "test": "node test/check-shaders.mjs && node test/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
